services:
  satsu:
    build: .
    ports:
      - "8400:8400"
    volumes:
      - satsu-data:/data
    environment:
      - TZ=Europe/Paris
      - SATSU_LOG_LEVEL=INFO
    restart: unless-stopped

volumes:
  satsu-data:
